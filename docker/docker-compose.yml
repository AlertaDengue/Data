version: "3.4"
services:
    demo_db:
      build:
        context: ".."
        dockerfile: "docker/Dockerfile"
      env_file:
        - ../.env
      hostname: demo_db
      ports:
        - "25432:25432"
      volumes:
        - ../:/Data
